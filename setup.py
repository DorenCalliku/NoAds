#
# TO BE UPDATED
#
#try:
# basic math and else
import numpy
import pandas 
import scipy
import itertools

# preprocessing
import sklearn

# plotting
import matplotlib

print("\nEverything seems OK. No need for setup!\n")

'''
except ImportError:
	answer = input("\nThe code requires numpy and other libraries to be installed. Would you like to install them? [Y/n]\n")
	if answer in ('yes', 'y', 'Y', 'YES', 'Yes'):
		try:
		    main(['install', 'numpy'])
		    main(['install', 'pandas'])
		    main(['install', 'scipy'])
		    main(['install', 'itertools'])
		    main(['install', 'sklearn'])
		    main(['install', 'lxml'])
		    main(['install', 'nltk'])
		    main(['install', 'ipywidgets'])
		    main(['install', 'bs4'])
		    main(['install', 'elasticsearch'])
		    main(['install', 'matplotlib'])
		    print("\nSetup complete! Good luck with your study!\n")
		except Exception as e: print("Something went wrong:\n" + e)
	else:
		print("\nSorry! The libraries could not be installed. You can install them manually.Best of luck!\n")
'''
